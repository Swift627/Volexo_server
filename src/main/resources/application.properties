# -------------------------------------------
# ? Application Info
# -------------------------------------------
spring.application.name=uberApp
server.port=8080

# -------------------------------------------
# ? Database Configuration
# -------------------------------------------
spring.datasource.url=jdbc:postgresql://localhost:5432/volexo
spring.datasource.username=postgres
spring.datasource.password=Sama@123

# Hibernate DDL mode: create-drop (?? drops data on every restart)
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=validate


# -------------------------------------------
# ? Hibernate SQL Logging (Uncomment to Enable)
# -------------------------------------------
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql=TRACE

# -------------------------------------------
# ? Optional: SQL Initialization Settings
# -------------------------------------------
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always
# spring.sql.init.data-locations=classpath:data.sql

# -------------------------------------------
# ? JWT Configuration
# -------------------------------------------
jwt.secretKey=alsghqwertyuiopkjvftyjnbvcdsxcvghjkmnhytrdeszxcvghjiolkmn

# -------------------------------------------
# ? Email (SMTP) Configuration - Gmail
# -------------------------------------------
spring.mail.username=${MAIL_USERNAME:samadhanghorpade173@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lyxy ckft qeet ukuf}
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Alternative Email Credentials (commented out)
# spring.mail.username=cena16524@gmail.com
# spring.mail.password=ffiu kawp bhvb tirr

# -------------------------------------------
# ? CORS Debug Logging
# -------------------------------------------
logging.level.org.springframework.web.cors=DEBUG

# -------------------------------------------
# ? Swagger/OpenAPI Documentation
# -------------------------------------------
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true


